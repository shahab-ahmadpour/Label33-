@{
    // Storefront signup is phone OTP — same as login.
    var returnUrl = Context.Request.Query["returnUrl"].ToString();
    Context.Response.Redirect(Url.Action("Login", "Account", new { returnUrl })!);
}
